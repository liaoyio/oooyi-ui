export default function TierPricing() {
  return (
    <section
      id="tiers"
      className="mx-auto flex w-full max-w-6xl scroll-mt-28 flex-col items-center gap-4 px-5 py-20 text-center sm:px-6 md:py-28"
    >
      <h2 className="text-balance font-runde text-3xl font-bold tracking-tight sm:text-4xl">
        Sponsorship is not available yet
      </h2>
      <p className="max-w-xl text-balance font-medium text-muted-foreground sm:text-lg">
        ooOYi UI does not currently offer paid sponsorships. Any sponsors or
        payment links from the original Rare UI project are separate from this
        registry.
      </p>
    </section>
  );
}
